export { renderProfile };

// obtindrer el perfil de el usuari actual
function renderProfile () => {
    const profileData = await getData("profile"); 
    const html = `
    
    `;
  
    return divProfile;
};